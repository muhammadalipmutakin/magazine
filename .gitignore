# Node.js dependencies (tidak perlu diupload)
node_modules/

# Next.js build output (otomatis dihasilkan oleh Vercel saat deploy)
.next/
out/
build/

# Log files (tidak diperlukan)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables (berisi data sensitif)
.env*
!.env.example  # Jika ingin menyertakan template env

# MacOS & Editor files (file lokal yang tidak relevan)
.DS_Store
*.swp
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel deployment cache (tidak perlu diupload)
.vercel/

# TypeScript cache files
*.tsbuildinfo
next-env.d.ts

# Coverage (jika menggunakan testing)
coverage/

# Yarn dan pnpm cache (jika menggunakan Yarn atau pnpm)
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.pnpm-debug.log*

.vercel
